name: Workflow for Feature Branch
on:
  push:
    branches: [feature/**]
jobs:
    Build:
      runs-on: ubuntu-lastest
      steps:
        - name: Check new feature quality
          run: echo "Start unit testing"
