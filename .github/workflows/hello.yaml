name: Hello World
on: 
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  hello-job:
    runs-on: ubuntu-lastest
    steps:
     - name: Run a one-line script
       run: echo 'Hello Github Actions!"
  Second-job:
    runs-on: windows-latest
    steps:
      - run: echo "This job is running on ${{ runnrt.os }}"
